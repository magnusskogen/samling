
    <div class="border-t border-current bg-white">
        <a href="#" class="flex js-accordion-link justify-between px-4 h-16 items-center w-full">
            <div class="strong">oster</div>
            <svg class="caret" width="16" height="10" viewBox="0 0 16 10" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M1 1L8 8L15 1" stroke="#072F19" stroke-width="2"/>
            </svg>
        </a>
        <div class="js-accordion-content relative overflow-hidden">
            {% for product in collections['oster'].products %}
              {% include 'productloop' %}
            {% endfor %}
        </div>
    </div>
    <div class="border-t border-current bg-white">
        <a href="#" class="flex js-accordion-link justify-between px-4 h-16 items-center w-full">
            <div class="strong">kjøtt</div>
            <svg class="caret" width="16" height="10" viewBox="0 0 16 10" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M1 1L8 8L15 1" stroke="#072F19" stroke-width="2"/>
            </svg>
        </a>
        <div class="js-accordion-content relative overflow-hidden">
            {% for product in collections['kjott'].products %}
              {% include 'productloop' %}
            {% endfor %}
        </div>
    </div>
    <div class="border-t border-current bg-white">
        <a href="#" class="flex js-accordion-link justify-between px-4 h-16 items-center w-full">
            <div class="strong">tørrvarer</div>
            <svg class="caret" width="16" height="10" viewBox="0 0 16 10" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M1 1L8 8L15 1" stroke="#072F19" stroke-width="2"/>
            </svg>
        </a>
        <div class="js-accordion-content relative overflow-hidden">
            {% for product in collections['torrvarer'].products %}
              {% include 'productloop' %}
            {% endfor %}
        </div>
    </div>
    <div class="border-t border-current bg-white">
        <a href="#" class="flex js-accordion-link justify-between px-4 h-16 items-center w-full">
            <div class="strong">tilbehør</div>
            <svg class="caret" width="16" height="10" viewBox="0 0 16 10" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M1 1L8 8L15 1" stroke="#072F19" stroke-width="2"/>
            </svg>
        </a>
        <div class="js-accordion-content relative overflow-hidden">
            {% for product in collections['tilbehor'].products %}
              {% include 'productloop' %}
            {% endfor %}
        </div>
    </div>
    <div class="border-t border-b border-current" style="background: #DFF9E3">
        <a href="#" class="flex js-accordion-link justify-between px-4 h-16 items-center w-full">
            <div class="strong">pakker</div>
            <svg class="caret" width="16" height="10" viewBox="0 0 16 10" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M1 1L8 8L15 1" stroke="#072F19" stroke-width="2"/>
            </svg>
        </a>
        <div class="js-accordion-content relative overflow-hidden">
          {% for product in collections['pakker'].products %}
              {% include 'productloop' %}
            {% endfor %}
          </div>
    </div>
